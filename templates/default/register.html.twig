{% extends "base.html.twig" %}

{% block body %}
    <div class="row">
        <div class="col-sm-2"></div>
        <div class="col">
            <h3>Register Form</h3>
        </div>
    </div>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
{% endblock %}